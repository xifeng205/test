#include <stdio.h>
#include <string.h>

typedef struct item_t{
  char *key;
  char *val;
} item;

//Remove field on the right side of space
char *strtrumr(char *str) {
  int len = strlen(str) -1;
  for(int i = 0; i < len; ++i) {
     
  }
  
  return str;
}
